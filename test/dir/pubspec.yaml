name: read_pubspec
version: 1.0.0
description: A sample project for testing pubspec.yaml parsing.
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  http: ^0.13.0
  path: ^1.8.0
dev_dependencies:
  test: ^1.16.0